project_id = "yabzmahzobwuwxylcwoy"

[functions.send-due-reminders]
verify_jwt = false

[functions.tmdb-proxy]
verify_jwt = false

[functions.send-contact-email]
# Require a valid Supabase JWT (anon key is fine for anonymous submitters)
# so random internet bots cannot hammer this endpoint directly.
verify_jwt = true

[functions.streaming-availability]
# Require a valid Supabase JWT (anon key is fine for guests browsing titles)
# so the Watchmode quota cannot be drained by random external callers.
verify_jwt = true

[functions.ai-recommendations]
# Personalized AI picks: a valid Supabase JWT is required so only the
# signed-in user can ask for (and pay for) their own recommendations.
verify_jwt = true
